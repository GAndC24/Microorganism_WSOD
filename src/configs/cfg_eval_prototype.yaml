RUNTIME:
  SEED: 42
  DEVICE: "cuda"  # 'cuda' or 'cpu'

DATA:
  DATASET_NAME: "voc_urinary_sediment"
  BATCH_SIZE: 32
  IMG_SIZE: 384
  NUM_CLASSES: 7

MODEL:
  IN_C: 512
  EMBED_DIM : 512
  PATCH_SIZE: 3
  ROI_OUT_SIZE: 7
  SPATIAL_SCALE: 0.125
  BACKBONE_WEIGHTS_PATH: "results/model_weights/backbone_weight_gmm_260609.pth"
  PATCH_EMBED_WEIGHTS_PATH: "results/model_weights/patch_embed_weight_gmm_260609.pth"
  DATASET_MPS_PATH: "results/dataset_morphological_prototypes/dataset_MPs_gmm_260609.pth"